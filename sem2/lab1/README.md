# Лабораторная работа №1

## Тема

Введение в Spring Boot.

## Цель работы

Получить практические навыки работы со Spring Framework и научиться
создавать простое веб-приложение.

## Ход работы

Был создан Maven-проект на Java с использованием Spring Boot. В файле
`pom.xml` добавлена зависимость для создания веб-приложения.

В классе `MessageController` создан список для хранения сообщений и
реализованы следующие запросы:

- GET / - вывод сообщения «Привет, мир!»;
- GET /messages - получение всех сообщений;
- POST /messages - добавление нового сообщения;
- PUT /messages/{index} - изменение сообщения по индексу;
- DELETE /messages/{index} - удаление сообщения по индексу.

В качестве дополнительного задания были реализованы два метода:

- DELETE /messages - очистка списка сообщений;
- GET /messages/count - получение количества сообщений.

Для запуска приложения используется команда:

```shell
./mvnw spring-boot:run
```

После запуска приложение доступно по адресу `http://localhost:8080`.
Работа приложения была проверена с помощью HTTP-запросов. Добавление,
изменение, удаление и получение сообщений выполняются правильно.

## Вывод

В ходе лабораторной работы было создано простое REST-приложение на Spring
Boot. Были изучены основные аннотации REST-контроллера и реализованы
операции для работы со списком сообщений.
