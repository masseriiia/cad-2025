# Лабораторная работа №2

## Тема

Spring MVC.

## Цель работы

Получить практические навыки работы со Spring MVC и научиться создавать
веб-приложение с использованием модели, представления и контроллера.

## Ход работы

Был создан Maven-проект на Java с использованием Spring Boot. В файле
`pom.xml` добавлены зависимости Spring Web и Thymeleaf.

Для работы приложения были созданы следующие классы:

- `Task` - модель задачи;
- `TaskRepository` - хранение списка задач;
- `TaskService` - добавление, удаление и изменение задач;
- `TaskController` - обработка запросов пользователя.

В файле `index.html` создана страница, на которой можно добавлять задачи,
отмечать их выполненными и удалять.

В качестве дополнительного задания было добавлено подтверждение удаления.
При нажатии на кнопку удаления пользователь должен подтвердить действие.

Для запуска приложения используется команда:

```shell
./mvnw spring-boot:run
```

После запуска приложение доступно по адресу `http://localhost:8080`.

## Вывод

В ходе лабораторной работы было создано простое приложение для управления
списком задач. Были изучены основные части Spring MVC и работа шаблонов
Thymeleaf.
